import fs from 'fs'

<section>
  <SectionTitle>Type Guard</SectionTitle>
</section>

<section>
  <PreCode code={fs.readFileSync(__dirname + '/type-guard.ts')}></PreCode>
  <div className="fragment fade-in">
    Notice: The type guard does not affect the{' '}
    <span className="warn">runtime</span> behavior!
  </div>
</section>
