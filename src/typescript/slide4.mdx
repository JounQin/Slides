import fs from 'fs'

import { ExternalLink, PreCode } from '../components'

<section>
  <PreCode
    code={fs.readFileSync(__dirname + '/legacy2.ts')}
    language="typescript"
  ></PreCode>
</section>

<section>
  <div className="fragment fade-out error">Why? Any difference????</div>
  <div className="fragment fade-in">
    <span className="warn">My original understanding:</span>
    <p className="fragment fade-in">
      Maybe <code>type</code> is more specific than <code>interface</code>.
    </p>
    <p className="fragment fade-in">But then I find out it could be a bug</p>
  </div>
</section>

<section>
  <ExternalLink href="https://github.com/microsoft/TypeScript/issues/15300">
    Related Issue
  </ExternalLink>
  <p className="fragment fade-in">
    It seems more consistent after
    <br /> <code>3.6.0-dev.20190806</code>,
  </p>
  <p className="fragment fade-in">
    although I don&apos;t know which commit actually affect.
  </p>
</section>
